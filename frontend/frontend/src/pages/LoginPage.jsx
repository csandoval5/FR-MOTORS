import { LoginForm } from '../components/auth/LoginForm'

/**
 * Página de Inicio de Sesión
 * Renderiza el formulario de login con su layout completo
 */
export default function LoginPage() {
  return <LoginForm />
}

